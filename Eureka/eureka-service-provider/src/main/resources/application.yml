spring:
  application:
    name: first-service-provider
server:
  port: 7001
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/


