spring:
  application:
    name: first-service-invoke
server:
  port: 9000
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/


